package net.seninp.jmotif;

/**
 * JUnit 4 category for expensive integration-style tests (excluded from default
 * {@code mvn test}; run with {@code mvn test -Pslow-tests}).
 */
public interface SlowTests {
  // marker only
}
